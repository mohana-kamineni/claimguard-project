apiVersion: v1
kind: Service
metadata:
  name: expense
  labels:
    app: expense
spec:
  type: NodePort
  selector:
    app: expense
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
      name: http
