# Local development only. Copy to .env (gitignored).
# Kubernetes uses ConfigMap + Secret with the same keys.

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=claimguard
DATABASE_USER=claimguard
DATABASE_PASSWORD=change-me

POLICY_BASE_URL=http://localhost:8001
POLICY_HTTP_TIMEOUT_SECONDS=3

EXPENSE_HOST=0.0.0.0
EXPENSE_PORT=8000
